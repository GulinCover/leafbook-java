spring:
  profiles:
    active: prod
  application:
    name: service-mq-api
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        group: LEAFBOOK_GROUP
        server-addr: localhost:8848
        prefix: nacos-config-service-mq-api
        file-extension: yml