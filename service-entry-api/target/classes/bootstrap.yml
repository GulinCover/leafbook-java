spring:
  profiles:
    active: prod
  application:
    name: service-entry-api
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        group: LEAFBOOK_GROUP
        server-addr: localhost:8848
        prefix: nacos-config-service-entry-api
        file-extension: yml