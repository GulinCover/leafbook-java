spring:
  profiles:
    active: prod
  application:
    name: service-common-api
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        group: LEAFBOOK_GROUP
        server-addr: localhost:8848
        prefix: nacos-config-service-common-api
        file-extension: yml